allInOne:
  enabled: true
  # Remove the QUERY_BASE_PATH to use default root path /
  extraEnv: []
  resources:
    requests:
      cpu: "200m"
      memory: "512Mi"
    limits:
      cpu: "500m"
      memory: "1Gi"


query:
  service:
    type: ClusterIP

